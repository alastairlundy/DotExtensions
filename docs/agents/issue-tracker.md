# Issue Tracker Configuration

This repository uses GitHub Issues as its issue tracker.

## Workflow

- Issues are created and managed through the GitHub web interface or `gh` CLI
- The `to-issues`, `triage`, `to-prd`, and related skills will interact with GitHub Issues via the `gh` command-line tool
- All standard GitHub Issue features (labels, milestones, assignees, etc.) are supported